kind: Service
apiVersion: v1
metadata:
  name: profiling-container
  labels:
    app: profiling-container
spec:
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  selector:
    app: profiling-container