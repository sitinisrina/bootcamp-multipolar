kind: Service
apiVersion: v1
metadata:
  name: my-container
  labels:
    app: my-container
spec:
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  selector:
    app: my-container
    